const assert = require('assert');
const page = require ('../page-object')

describe('Annotation interface', () => {
    it('should display annotated entities', () => {

    });

    it('should change entity annotation', () => {

    });

    it('should delete entity annotation', () => {
        
    });
});